specVersion: "v1"
serviceId: "sales-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "sales-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
